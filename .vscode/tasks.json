{
    "tasks": [
        {
            "type": "shell",
            "label": "clang build active file",
            "command": "/usr/bin/clang",
            "args": [
                "-g",
                "${fileDirname}/*.c",
                "${workspaceFolder}/src/tree/tree.c",
                "${workspaceFolder}/src/linked_list/linked_list.c",
                "${workspaceFolder}/src/tree_traverse/tree_traverse.c",
                "-o",
                "${fileDirname}/${fileBasenameNoExtension}"
            ],
            "options": {
                "cwd": "/usr/bin"
            }
        }
    ],
    "version": "2.0.0"
}